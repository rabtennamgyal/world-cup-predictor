import logo from '../assets/logo/logo.svg';


function Header() {
    return (
        <div className="header">
            <img src={logo} alt='logo' />
        </div>
    )
};

export default Header;